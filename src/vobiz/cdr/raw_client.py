# This file was auto-generated by Fern from our API Definition.

import contextlib
import datetime as dt
import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import encode_path_param
from ..core.parse_error import ParsingError
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..errors.not_found_error import NotFoundError
from .types.export_cdrs_request_call_direction import ExportCdrsRequestCallDirection
from .types.get_cdr_response import GetCdrResponse
from .types.list_cdrs_request_call_direction import ListCdrsRequestCallDirection
from .types.list_cdrs_response import ListCdrsResponse
from .types.list_recent_cdrs_response import ListRecentCdrsResponse
from .types.search_cdrs_request_call_direction import SearchCdrsRequestCallDirection
from .types.search_cdrs_response import SearchCdrsResponse
from pydantic import ValidationError


class RawCdrClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list_cdrs(
        self,
        auth_id: str,
        *,
        from_number: typing.Optional[str] = None,
        to_number: typing.Optional[str] = None,
        start_date: typing.Optional[dt.date] = None,
        end_date: typing.Optional[dt.date] = None,
        call_direction: typing.Optional[ListCdrsRequestCallDirection] = None,
        min_duration: typing.Optional[int] = None,
        page: typing.Optional[int] = None,
        per_page: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[ListCdrsResponse]:
        """
        Returns all CDRs for your account. Supports filtering by phone numbers,
        date range, call direction, duration, and pagination.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        from_number : typing.Optional[str]
            Filter by the originating phone number (caller).

        to_number : typing.Optional[str]
            Filter by the destination phone number (callee).

        start_date : typing.Optional[dt.date]
            Beginning of the search period (YYYY-MM-DD). Required when using `end_date`.

        end_date : typing.Optional[dt.date]
            End of the search period (YYYY-MM-DD). Required when using `start_date`.

        call_direction : typing.Optional[ListCdrsRequestCallDirection]
            Filter by direction.

        min_duration : typing.Optional[int]
            Minimum call duration in seconds. Excludes calls shorter than this value.

        page : typing.Optional[int]
            Page number for paginated results.

        per_page : typing.Optional[int]
            Number of records per page. Max: 100.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[ListCdrsResponse]
            Paginated list of CDRs
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr",
            method="GET",
            params={
                "from_number": from_number,
                "to_number": to_number,
                "start_date": str(start_date) if start_date is not None else None,
                "end_date": str(end_date) if end_date is not None else None,
                "call_direction": call_direction,
                "min_duration": min_duration,
                "page": page,
                "per_page": per_page,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListCdrsResponse,
                    parse_obj_as(
                        type_=ListCdrsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def search_cdrs(
        self,
        auth_id: str,
        *,
        from_number: typing.Optional[str] = None,
        to_number: typing.Optional[str] = None,
        start_date: typing.Optional[dt.date] = None,
        end_date: typing.Optional[dt.date] = None,
        call_direction: typing.Optional[SearchCdrsRequestCallDirection] = None,
        min_duration: typing.Optional[int] = None,
        page: typing.Optional[int] = None,
        per_page: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[SearchCdrsResponse]:
        """
        Identical filters to the list endpoint, but the response also includes a
        `filter_summary` object describing the active filters applied.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        from_number : typing.Optional[str]
            Filter by the originating phone number (caller).

        to_number : typing.Optional[str]
            Filter by the destination phone number (callee).

        start_date : typing.Optional[dt.date]
            Beginning of the search period (YYYY-MM-DD). Required when using `end_date`.

        end_date : typing.Optional[dt.date]
            End of the search period (YYYY-MM-DD). Required when using `start_date`.

        call_direction : typing.Optional[SearchCdrsRequestCallDirection]
            Filter by direction.

        min_duration : typing.Optional[int]
            Minimum call duration in seconds. Excludes calls shorter than this value.

        page : typing.Optional[int]
            Page number for paginated results.

        per_page : typing.Optional[int]
            Number of records per page. Max: 100.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[SearchCdrsResponse]
            CDR list with active filter summary
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr/search",
            method="GET",
            params={
                "from_number": from_number,
                "to_number": to_number,
                "start_date": str(start_date) if start_date is not None else None,
                "end_date": str(end_date) if end_date is not None else None,
                "call_direction": call_direction,
                "min_duration": min_duration,
                "page": page,
                "per_page": per_page,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SearchCdrsResponse,
                    parse_obj_as(
                        type_=SearchCdrsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def list_recent_cdrs(
        self,
        auth_id: str,
        *,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[ListRecentCdrsResponse]:
        """
        Returns the most recent CDRs for your account without requiring a date range.
        Default 20 records; use `limit` to retrieve more.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        limit : typing.Optional[int]
            Number of recent CDRs to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[ListRecentCdrsResponse]
            Recent CDRs
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr/recent",
            method="GET",
            params={
                "limit": limit,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListRecentCdrsResponse,
                    parse_obj_as(
                        type_=ListRecentCdrsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    @contextlib.contextmanager
    def export_cdrs(
        self,
        auth_id: str,
        *,
        from_number: typing.Optional[str] = None,
        to_number: typing.Optional[str] = None,
        start_date: typing.Optional[dt.date] = None,
        end_date: typing.Optional[dt.date] = None,
        call_direction: typing.Optional[ExportCdrsRequestCallDirection] = None,
        min_duration: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Iterator[HttpResponse[typing.Iterator[bytes]]]:
        """
        Returns CDR data as a downloadable CSV file. Same filters as the list endpoint.

        **Note:** Do NOT send `Accept: application/json` on this endpoint - the response is `text/csv`.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        from_number : typing.Optional[str]
            Filter by the originating phone number (caller).

        to_number : typing.Optional[str]
            Filter by the destination phone number (callee).

        start_date : typing.Optional[dt.date]
            Beginning of the search period (YYYY-MM-DD). Required when using `end_date`.

        end_date : typing.Optional[dt.date]
            End of the search period (YYYY-MM-DD). Required when using `start_date`.

        call_direction : typing.Optional[ExportCdrsRequestCallDirection]
            Filter by direction.

        min_duration : typing.Optional[int]
            Minimum call duration in seconds. Excludes calls shorter than this value.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration. You can pass in configuration such as `chunk_size`, and more to customize the request and response.

        Returns
        -------
        typing.Iterator[HttpResponse[typing.Iterator[bytes]]]
            CSV file containing CDR rows
        """
        with self._client_wrapper.httpx_client.stream(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr/export",
            method="GET",
            params={
                "from_number": from_number,
                "to_number": to_number,
                "start_date": str(start_date) if start_date is not None else None,
                "end_date": str(end_date) if end_date is not None else None,
                "call_direction": call_direction,
                "min_duration": min_duration,
            },
            request_options=request_options,
        ) as _response:

            def _stream() -> HttpResponse[typing.Iterator[bytes]]:
                try:
                    if 200 <= _response.status_code < 300:
                        _chunk_size = request_options.get("chunk_size", None) if request_options is not None else None
                        return HttpResponse(
                            response=_response, data=(_chunk for _chunk in _response.iter_bytes(chunk_size=_chunk_size))
                        )
                    _response.read()
                    _response_json = _response.json()
                except JSONDecodeError:
                    raise ApiError(
                        status_code=_response.status_code, headers=dict(_response.headers), body=_response.text
                    )
                except ValidationError as e:
                    raise ParsingError(
                        status_code=_response.status_code,
                        headers=dict(_response.headers),
                        body=_response.json(),
                        cause=e,
                    )
                raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

            yield _stream()

    def get_cdr(
        self, auth_id: str, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[GetCdrResponse]:
        """
        Retrieve the CDR for a specific completed call using its `call_id`.
        Useful when you have a `call_id` from a callback or previous API response.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        call_id : str
            The unique call ID of the completed call.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[GetCdrResponse]
            CDR details
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr/{encode_path_param(call_id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    GetCdrResponse,
                    parse_obj_as(
                        type_=GetCdrResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawCdrClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list_cdrs(
        self,
        auth_id: str,
        *,
        from_number: typing.Optional[str] = None,
        to_number: typing.Optional[str] = None,
        start_date: typing.Optional[dt.date] = None,
        end_date: typing.Optional[dt.date] = None,
        call_direction: typing.Optional[ListCdrsRequestCallDirection] = None,
        min_duration: typing.Optional[int] = None,
        page: typing.Optional[int] = None,
        per_page: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[ListCdrsResponse]:
        """
        Returns all CDRs for your account. Supports filtering by phone numbers,
        date range, call direction, duration, and pagination.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        from_number : typing.Optional[str]
            Filter by the originating phone number (caller).

        to_number : typing.Optional[str]
            Filter by the destination phone number (callee).

        start_date : typing.Optional[dt.date]
            Beginning of the search period (YYYY-MM-DD). Required when using `end_date`.

        end_date : typing.Optional[dt.date]
            End of the search period (YYYY-MM-DD). Required when using `start_date`.

        call_direction : typing.Optional[ListCdrsRequestCallDirection]
            Filter by direction.

        min_duration : typing.Optional[int]
            Minimum call duration in seconds. Excludes calls shorter than this value.

        page : typing.Optional[int]
            Page number for paginated results.

        per_page : typing.Optional[int]
            Number of records per page. Max: 100.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[ListCdrsResponse]
            Paginated list of CDRs
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr",
            method="GET",
            params={
                "from_number": from_number,
                "to_number": to_number,
                "start_date": str(start_date) if start_date is not None else None,
                "end_date": str(end_date) if end_date is not None else None,
                "call_direction": call_direction,
                "min_duration": min_duration,
                "page": page,
                "per_page": per_page,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListCdrsResponse,
                    parse_obj_as(
                        type_=ListCdrsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def search_cdrs(
        self,
        auth_id: str,
        *,
        from_number: typing.Optional[str] = None,
        to_number: typing.Optional[str] = None,
        start_date: typing.Optional[dt.date] = None,
        end_date: typing.Optional[dt.date] = None,
        call_direction: typing.Optional[SearchCdrsRequestCallDirection] = None,
        min_duration: typing.Optional[int] = None,
        page: typing.Optional[int] = None,
        per_page: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[SearchCdrsResponse]:
        """
        Identical filters to the list endpoint, but the response also includes a
        `filter_summary` object describing the active filters applied.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        from_number : typing.Optional[str]
            Filter by the originating phone number (caller).

        to_number : typing.Optional[str]
            Filter by the destination phone number (callee).

        start_date : typing.Optional[dt.date]
            Beginning of the search period (YYYY-MM-DD). Required when using `end_date`.

        end_date : typing.Optional[dt.date]
            End of the search period (YYYY-MM-DD). Required when using `start_date`.

        call_direction : typing.Optional[SearchCdrsRequestCallDirection]
            Filter by direction.

        min_duration : typing.Optional[int]
            Minimum call duration in seconds. Excludes calls shorter than this value.

        page : typing.Optional[int]
            Page number for paginated results.

        per_page : typing.Optional[int]
            Number of records per page. Max: 100.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[SearchCdrsResponse]
            CDR list with active filter summary
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr/search",
            method="GET",
            params={
                "from_number": from_number,
                "to_number": to_number,
                "start_date": str(start_date) if start_date is not None else None,
                "end_date": str(end_date) if end_date is not None else None,
                "call_direction": call_direction,
                "min_duration": min_duration,
                "page": page,
                "per_page": per_page,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SearchCdrsResponse,
                    parse_obj_as(
                        type_=SearchCdrsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def list_recent_cdrs(
        self,
        auth_id: str,
        *,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[ListRecentCdrsResponse]:
        """
        Returns the most recent CDRs for your account without requiring a date range.
        Default 20 records; use `limit` to retrieve more.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        limit : typing.Optional[int]
            Number of recent CDRs to return.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[ListRecentCdrsResponse]
            Recent CDRs
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr/recent",
            method="GET",
            params={
                "limit": limit,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListRecentCdrsResponse,
                    parse_obj_as(
                        type_=ListRecentCdrsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    @contextlib.asynccontextmanager
    async def export_cdrs(
        self,
        auth_id: str,
        *,
        from_number: typing.Optional[str] = None,
        to_number: typing.Optional[str] = None,
        start_date: typing.Optional[dt.date] = None,
        end_date: typing.Optional[dt.date] = None,
        call_direction: typing.Optional[ExportCdrsRequestCallDirection] = None,
        min_duration: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.AsyncIterator[AsyncHttpResponse[typing.AsyncIterator[bytes]]]:
        """
        Returns CDR data as a downloadable CSV file. Same filters as the list endpoint.

        **Note:** Do NOT send `Accept: application/json` on this endpoint - the response is `text/csv`.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        from_number : typing.Optional[str]
            Filter by the originating phone number (caller).

        to_number : typing.Optional[str]
            Filter by the destination phone number (callee).

        start_date : typing.Optional[dt.date]
            Beginning of the search period (YYYY-MM-DD). Required when using `end_date`.

        end_date : typing.Optional[dt.date]
            End of the search period (YYYY-MM-DD). Required when using `start_date`.

        call_direction : typing.Optional[ExportCdrsRequestCallDirection]
            Filter by direction.

        min_duration : typing.Optional[int]
            Minimum call duration in seconds. Excludes calls shorter than this value.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration. You can pass in configuration such as `chunk_size`, and more to customize the request and response.

        Returns
        -------
        typing.AsyncIterator[AsyncHttpResponse[typing.AsyncIterator[bytes]]]
            CSV file containing CDR rows
        """
        async with self._client_wrapper.httpx_client.stream(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr/export",
            method="GET",
            params={
                "from_number": from_number,
                "to_number": to_number,
                "start_date": str(start_date) if start_date is not None else None,
                "end_date": str(end_date) if end_date is not None else None,
                "call_direction": call_direction,
                "min_duration": min_duration,
            },
            request_options=request_options,
        ) as _response:

            async def _stream() -> AsyncHttpResponse[typing.AsyncIterator[bytes]]:
                try:
                    if 200 <= _response.status_code < 300:
                        _chunk_size = request_options.get("chunk_size", None) if request_options is not None else None
                        return AsyncHttpResponse(
                            response=_response,
                            data=(_chunk async for _chunk in _response.aiter_bytes(chunk_size=_chunk_size)),
                        )
                    await _response.aread()
                    _response_json = _response.json()
                except JSONDecodeError:
                    raise ApiError(
                        status_code=_response.status_code, headers=dict(_response.headers), body=_response.text
                    )
                except ValidationError as e:
                    raise ParsingError(
                        status_code=_response.status_code,
                        headers=dict(_response.headers),
                        body=_response.json(),
                        cause=e,
                    )
                raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

            yield await _stream()

    async def get_cdr(
        self, auth_id: str, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[GetCdrResponse]:
        """
        Retrieve the CDR for a specific completed call using its `call_id`.
        Useful when you have a `call_id` from a callback or previous API response.

        Parameters
        ----------
        auth_id : str
            Your account Auth ID

        call_id : str
            The unique call ID of the completed call.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[GetCdrResponse]
            CDR details
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/v1/Account/{encode_path_param(auth_id)}/cdr/{encode_path_param(call_id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    GetCdrResponse,
                    parse_obj_as(
                        type_=GetCdrResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
